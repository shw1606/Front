import React from "react";
import {SearchButtonWrapper, SearchIcon} from "./style";

const SearchButton = () => {
    return (
      <SearchButtonWrapper>
          <SearchIcon/>
      </SearchButtonWrapper>
    );
};

export default SearchButton;